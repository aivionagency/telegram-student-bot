# Инициализируем Git в твоем проекте
git init -b main

# Добавляем все файлы в "область подготовки", кроме тех, что в .gitignore
git add .

# Фиксируем изменения (создаем "точку сохранения")
git commit -m "Initial commit: Рабочая версия с расписанием и ДЗ"

# Связываем твой локальный проект с репозиторием на GitHub
git remote add origin https://github.com/ТВОЙ_НИК/telegram-student-bot.git

# Отправляем твой код на GitHub
git push -u origin main

token.pickle